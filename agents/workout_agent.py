def generate_workout_plan(profile):
    """
    TODO: Integrate with an LLM to generate a personalized workout routine.
    Placeholder implementation returns a stub.
    """
    return {
        "summary": f"Custom workout routine for goal: {profile.fitness_goal}",
        "sessions": [
            # e.g. {"day": 1, "exercises": [{"name": "...", "sets": 3, "reps": 10}, ...]}
        ]
    }